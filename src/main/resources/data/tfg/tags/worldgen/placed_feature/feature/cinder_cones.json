{
    "values": ["tfg:earth/pumice_patch"],
    "replace": false,
    "__comment__": "The new worldgen uses different noise for cinder cones than the old worldgen, so this requires different features"
}
